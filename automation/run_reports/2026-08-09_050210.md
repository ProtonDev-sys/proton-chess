# Proton Chess run 2026-08-09_050210

- Status: passed
- Change kept: canonical validation restoration
- Base commit: `282f012`
- Engine binary: `build/Release/proton_chess.exe`

## Why this run came first

The merged 0.2.0 engine is a replacement architecture. It builds and passes its native tests, but the merge removed the project-level perft and python-chess legality validators. It also removed the `moves` debug command those checks used and changed the perft output format. Under the project priority order, restoring reproducible correctness checks comes before search or evaluation tuning.

The original dirty checkout was not rebased across the replacement. A clean linked worktree was created from `origin/main`, leaving every existing local edit intact.

## Failing baseline

- Legacy perft validator: failed to find a result because 0.2.0 emits `info string perft depth 1 nodes 20 ...`.
- Legacy move-generation cross-check: failed with `OURS: []` against non-empty python-chess legal move sets because 0.2.0 no longer handled `moves`.

## Change

- Restored a sorted `moves` debug command in `src/engine/uci.cpp`.
- Added async UCI smoke coverage for the 20 legal start-position moves and `perft 2 = 400`.
- Restored perft and legal-move cross-check scripts with timeouts and robust output parsing.
- Restored five perft cases and nine curated move-generation positions.
- Added the minimal Python dependency declaration and package marker required by the canonical compile command.
- Added a GitHub Actions matrix that builds and validates on Windows and Ubuntu.

## Passing evidence

- `ctest --test-dir build -C Release --output-on-failure`: 2/2 passed.
- `python -m compileall python tools`: passed.
- `python tools/check_perft.py build/Release/proton_chess.exe`: 5/5 passed.
- `python tools/crosscheck_movegen.py build/Release/proton_chess.exe --count 40 --plies 10 --seed 17`: 49/49 positions matched python-chess.
- `git diff --check`: passed before reporting.

## Scope

This run changes validation and a non-standard debug command only. Search, evaluation, time management, opening choice, and human-style move selection are unchanged. No Elo or playing-strength claim is made.
